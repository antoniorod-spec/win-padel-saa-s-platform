# v0 runtime files (should only exist in preview, not in git)
__v0_runtime_loader.js
__v0_devtools.tsx
__v0_jsx-dev-runtime.ts

# Common ignores
node_modules/
.next/
.env*.local
.DS_Store
.vercel

# Environment files (NEVER commit these)
.env
.env.backup
.env.temp

# Prisma generated files are in node_modules
lib/generated/

# Lock files - using npm
pnpm-lock.yaml
yarn.lock

# Local scratch
tmp/

# Local uploads (fallback cuando Supabase no está configurado)
public/uploads/
